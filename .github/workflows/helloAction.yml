name: C/C++ CI

on:
  push:
      branches: [ testCodespaces2 ]
  pull_request:
      branches: [ testCodespaces2 ]

jobs:
  build:
    name: BUILD
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: build
    run: ./Singleton/build.sh
    
    